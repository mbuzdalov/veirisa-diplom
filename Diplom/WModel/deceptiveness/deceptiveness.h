#ifndef DIPLOM_RUGGEDNESS_H
#define DIPLOM_RUGGEDNESS_H

#include "source.h"

struct deceptiveness {

    static size_t apply_ruggedness(size_t num, size_t s, size_t f);
    static size_t size_after_ruggedness(size_t num, size_t size);
};


#endif //DIPLOM_RUGGEDNESS_H
